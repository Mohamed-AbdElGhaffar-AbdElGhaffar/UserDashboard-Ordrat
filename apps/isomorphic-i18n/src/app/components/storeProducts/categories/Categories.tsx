'use client'
import React from 'react';

export default function StoreCategories({lang}:{lang?:string;}) {
  const text = {
    title: lang === 'ar' ? 'شكل البانر' : 'Banner Design',
  };
  
  return <>
    
  </>
}